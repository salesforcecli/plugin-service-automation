{
  "require": ["ts-node/register"],
  "spec": ["test/**/*.test.ts"],
  "watch-extensions": "ts",
  "recursive": true,
  "reporter": "spec",
  "timeout": 600000,
  "node-option": ["loader=ts-node/esm"]
}
